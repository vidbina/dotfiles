[user]
	name = David Asabina
	email = vid@bina.me

[color]
	ui = true

[color.grep]
  match = black red
  filename = blue
  linenumber = yellow

[core]
	editor = nvim
	excludesfile = ~/.gitignore

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
[gpg]
	program = gpg2

[sendemail]
  annotate = true
  smtpServer = /run/current-system/sw/bin/msmtp
  smtpServerOption = -a
  smtpServerOption = vidbina

# vim: syntax=gitconfig
[init]
	defaultBranch = main
